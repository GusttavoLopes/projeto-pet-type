import { StyleSheet } from "react-native";

export default StyleSheet.create({
  view: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  text: {
    fontSize: 18,
    textAlign: "center",
    marginHorizontal: 40,
  },
  image: {
    width: 300,
    height: 300,
  },
});
